spring:
  datasource:
    url: jdbc:h2:mem:keynotes-db
  h2:
    console:
      enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/controle-realm
          jwk-set-uri: http://localhost:8080/realms/controle-realm/protocol/openid-connect/certs